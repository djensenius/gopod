apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - pvc.yaml
  - cronjob.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: gopod
      app.kubernetes.io/version: 0.1.0
